const main = "/api/auth";

const AuthApiConstants = {
    main: `${main}/management`,
    login: `${main}/management/login`,
    logout: `${main}/management/logout`,
    refresh: `${main}/management/refresh`,
    oauth: `${main}/management/oauth`,
};

export default AuthApiConstants;